<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Hiep's Site</title>
    <style>
        .contaner {
            max-width: 960px;
            margin: auto;
            display: flex;
            justify-content: center;
        }
        .contaner .content-mail {
            width: 50%;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
            text-align: center;
            height: 450px;
        }
        .button {
            width: 120px;
            height: 40px;
            border: 1px solid skyblue;
            border-radius: 5px;
            color: white;
            text-align: center;
            
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">Confirm Account</div>
        <div class="content-mail">
            <a href="{{url}}" class="button">Confirm account</a>
        </div>
    </div>
</body>
</html>
